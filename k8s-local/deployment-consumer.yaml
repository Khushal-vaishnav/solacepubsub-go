apiVersion: apps/v1
kind: Deployment
metadata:
  name: solace-consumer
  namespace: solace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: solace-consumer
  template:
    metadata:
      labels:
        app: solace-consumer
    spec:
      containers:
      - name: consumer
        image: solace-consumer:local
        envFrom:
        - configMapRef:
            name: solace-app-config
        - secretRef:
            name: solace-app-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"

